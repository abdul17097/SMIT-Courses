// Logical operator
// && : and operator
// || : or operator
// ! : not opertor

let obtainMarks = 500;
let totalMarks = 1100;

// AND operator
// true && true : true
// true && false: false
// false && false: fasle
// console.log(obtainMarks > 600 && obtainMarks < totalMarks);

// OR operator
// true && true : true
// true && false: true
// false && true: true
// false && false: fasle
// console.log(obtainMarks > 600 || obtainMarks < totalMarks); // false || true : true

let x = !(10 > 20);
console.log(x);
